{
  "name": "copy-path-tools",
  "displayName": "Copy Path Tools",
  "description": "Copy file name, path, and line info from tabs and editor context menus.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copy-path-tools.copyFileNameTab",
        "title": "Copy File Name"
      },
      {
        "command": "copy-path-tools.copyFileNameNoExtTab",
        "title": "Copy File Name Without Extension"
      },
      {
        "command": "copy-path-tools.copyFileNameLineEditor",
        "title": "Copy File Name with Line Number"
      },
      {
        "command": "copy-path-tools.copyRelativePathLineEditor",
        "title": "Copy Relative Path with Line Number"
      },
      {
        "command": "copy-path-tools.copyFullPathLineEditor",
        "title": "Copy Full Path with Line Number"
      }
    ],
    "menus": {
      "editor/title/context": [
        {
          "command": "copy-path-tools.copyFileNameTab",
          "group": "1_cutcopypaste@100",
          "when": "resourceScheme == 'file'"
        },
        {
          "command": "copy-path-tools.copyFileNameNoExtTab",
          "group": "1_cutcopypaste@101",
          "when": "resourceScheme == 'file'"
        }
      ],
      "editor/context": [
        {
          "command": "copy-path-tools.copyFileNameLineEditor",
          "group": "pathlncopy@100",
          "when": "resourceScheme == 'file'"
        },
        {
          "command": "copy-path-tools.copyRelativePathLineEditor",
          "group": "pathlncopy@101",
          "when": "resourceScheme == 'file'"
        },
        {
          "command": "copy-path-tools.copyFullPathLineEditor",
          "group": "pathlncopy@102",
          "when": "resourceScheme == 'file'"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "check-types": "tsc --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/vscode": "^1.103.0",
    "@types/node": "22.x",
    "@typescript-eslint/eslint-plugin": "^8.39.0",
    "@typescript-eslint/parser": "^8.39.0",
    "eslint": "^9.32.0",
    "esbuild": "^0.25.8",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.2"
  }
}
